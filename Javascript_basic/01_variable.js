name = 'Mike'; // `name`이란 변수에는 `"Mike"`라는 값이 들어감, `Mike`는 `""(따옴표)`로 감싸져 있는데, Javascript에서는 문자(열)은 항상 `"",''(따옴표)`로 감싸줘야 함. `',"(따옴표)`로 감싸주지 않으면 error가 발생함.
age = 30; // `age`란 변수에는 `30`이란 값이 들어감. 줄 마지막에 들어간 `;(세미클론)`은 한 줄이 끝났다는 의미로 써줌. 생략해도 되지만, 항상 적어주는 것이 좋음

alert(name);
console.log(age);

//----------------------

let name = 'Mike';

// 1000 lines...

//let name = "google";  // 이미 선언되었다고 에러가 발생함. 따라서, 다른 사람은 이미 사용하고 있는 변수명이라는 것을 인지하여 변수명을 바꿈
